<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.shop.branch.dal.dao.ShopBranchDao">
    
	<sql id="shopBranchColumns">
		a.id AS "id",
		a.shop_id AS "shop.id",
		a.circle_id AS "circle.id",
		a.market_id AS "market.id",
		a.market_floor_id AS "marketFloor.id",
		a.side AS "side",
		a.name AS "name",
		a.location_detail AS "locationDetail",
		a.contact_name AS "contactName",
		a.phone AS "phone",
		a.remarks AS "remarks",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="shopBranchJoins">
	</sql>
    
	<select id="get" resultType="ShopBranch">
		SELECT 
			<include refid="shopBranchColumns"/>
		FROM shop_branch a
		<include refid="shopBranchJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ShopBranch">
		SELECT 
			<include refid="shopBranchColumns"/>
		FROM shop_branch a
		<include refid="shopBranchJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ShopBranch">
		SELECT 
			<include refid="shopBranchColumns"/>
		FROM shop_branch a
		<include refid="shopBranchJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO shop_branch(
			id,
			shop_id,
			circle_id,
			market_id,
			market_floor_id,
			side,
			name,
			location_detail,
			contact_name,
			phone,
			remarks,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag
		) VALUES (
			#{id},
			#{shop.id},
			#{circle.id},
			#{market.id},
			#{marketFloor.id},
			#{side},
			#{name},
			#{locationDetail},
			#{contactName},
			#{phone},
			#{remarks},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE shop_branch SET 	
			shop_id = #{shop.id},
			circle_id = #{circle.id},
			market_id = #{market.id},
			market_floor_id = #{marketFloor.id},
			side = #{side},
			name = #{name},
			location_detail = #{locationDetail},
			contact_name = #{contactName},
			phone = #{phone},
			remarks = #{remarks},
			update_by = #{updateBy.id},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE shop_branch SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>