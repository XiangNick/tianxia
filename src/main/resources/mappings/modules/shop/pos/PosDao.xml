<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.shop.pos.dal.dao.PosDao">
    
	<sql id="posColumns">
		a.id AS "id",
		a.pos_no AS "posNo",
		a.shop_id AS "shop.id",
		a.branch_id AS "branch.id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		s.name AS "shop.name",
		b.name AS "branch.name"
	</sql>
	
	<sql id="posJoins">
		join shop s on a.shop_id = s.id
		join shop_branch b on a.branch_id = b.id
	</sql>
    
	<select id="get" resultType="Pos">
		SELECT 
			<include refid="posColumns"/>
		FROM pos a
		<include refid="posJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Pos">
		SELECT 
			<include refid="posColumns"/>
		FROM pos a
		<include refid="posJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Pos">
		SELECT 
			<include refid="posColumns"/>
		FROM pos a
		<include refid="posJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO pos(
			id,
			pos_no,
			shop_id,
			branch_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{posNo},
			#{shop.id},
			#{branch.id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE pos SET 	
			pos_no = #{posNo},
			shop_id = #{shop.id},
			branch_id = #{branch.id},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE pos SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

	<select id="findListByShopId" resultType="com.thinkgem.jeesite.modules.shop.pos.dal.domain.Pos">
		SELECT
		<include refid="posColumns"/>
		FROM pos a
		<include refid="posJoins"/>
		WHERE
		a.shop_id = #{shop.id}
	</select>
	
</mapper>